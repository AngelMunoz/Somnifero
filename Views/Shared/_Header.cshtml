@model Somnifero.ViewModels.HeaderData
<header class="som-header">
    <nav class="uk-navbar-container" uk-navbar>
        <section class="uk-navbar-left">
            <ul class="uk-navbar-nav">
                <li class="uk-active"><a href="/">Somnifero</a></li>
            </ul>
        </section>
        <section class="uk-navbar-right">
            <ul class="uk-navbar-nav">
                @if (User.Identity.IsAuthenticated)
                {
                    <li class="uk-active"><a href="/auth/signout">Sign Out</a></li>
                }

            </ul>
        </section>
    </nav>
</header>